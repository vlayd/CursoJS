/* Escreva uma função chamada ePaisagem que recebe dois
argumentos, largura e altura de uma imagem (number).
Retorne ture se a imagem estiver no modo paisagem
*/
const ePaisagem = (l, a) => l > a;
console.log('A foto é paisagem', ePaisagem(1080, 780));